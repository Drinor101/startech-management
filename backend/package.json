{
  "name": "startech-backend",
  "version": "1.0.0",
  "description": "Startech Backend API",
  "main": "server.js",
  "scripts": {
    "start": "node --expose-gc --max-old-space-size=512 server.js",
    "dev": "nodemon --expose-gc server.js",
    "build": "echo 'No build step required'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "render-postbuild": "echo 'Post-build completed'"
  },
  "keywords": ["startech", "api", "backend"],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "dotenv": "^16.3.1",
    "@supabase/supabase-js": "^2.38.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}